# 订单功能修复 - 快速测试指南

## 测试日期
2026-04-26

## 测试前准备

1. 打开浏览器，访问 `http://localhost:3000`
2. 确保后端服务正常运行
3. 清除浏览器缓存（可选，但推荐）

---

## 测试1：账号切换与订单过滤

### 测试步骤

1. **打开门店端页面**
   - 访问 `m-index.html`
   - 使用账号登录（默认海淀区店 ST002）

2. **创建测试订单**
   - 打开新的浏览器标签页
   - 访问 `c-index.html`（C端）
   - 选择服务（如：皮具护理 + 床单被罩）
   - 选择门店（海淀区干洗店 ST002）
   - 选择配送方式（自送到店）
   - 填写信息并提交订单
   - 完成支付

3. **检查订单同步**
   - 回到 m-index 页面
   - 等待8秒或手动刷新
   - 确认新订单出现在列表中
   - 订单物品显示为"皮具护理"和"床单被罩"

4. **切换门店账号**
   - 点击右上角用户头像
   - 选择"切换账号"
   - 选择"朝阳区干洗店"或"西城区干洗店"
   - 确认切换

5. **验证过滤效果**
   - 订单列表应只显示属于当前门店的订单
   - 海淀区店的订单不应该在朝阳区店账号下显示
   - 如果有属于当前门店的订单，应正确显示

### 预期结果
✅ 切换账号后，订单列表正确过滤，只显示当前门店的订单  
✅ 订单物品信息与C端下单时一致  
✅ 账户信息窗口可以正常关闭

### 如果出现问题
❌ 如果订单没有过滤 → 检查 `loadOrderList` 函数的过滤逻辑  
❌ 如果看不到订单 → 检查 `syncC端Orders` 是否正常工作  
❌ 如果物品信息不一致 → 检查 `confirmOrder` 中的 `items` 字段设置

---

## 测试2：物品状态自动同步订单状态

### 测试步骤

1. **查看待处理订单**
   - 在 m-index 中找到状态为"待处理"或"已支付"的订单
   - 点击"查看"打开订单详情

2. **更新物品状态**
   - 在"物品状态管理"区域
   - 将物品状态从"待处理"改为"清洗中"
   - 点击确认

3. **观察自动同步**
   - 应弹出提示："物品状态已更新为「清洗中」"
   - 应同时显示："✓ 物品开始清洗，订单状态自动更新为清洗中"
   - 点击确定后，订单列表中的订单状态应变为"清洗中"

4. **测试多物品同步**
   - 找一个包含多个物品的订单
   - 将第一个物品状态更新为"已清洗"
   - 订单状态不应变化（因为不是所有物品都完成）
   - 将所有物品都更新为"已清洗"
   - 订单状态应自动变为"清洗完成"

5. **测试完成流程**
   - 将所有物品更新为"待取件"
   - 订单状态应变为"待取件"
   - 将所有物品更新为"已完成"
   - 订单状态应变为"已完成"

### 预期结果
✅ 单物品订单：物品状态变更自动同步订单状态  
✅ 多物品订单：所有物品达到同一状态时自动同步订单状态  
✅ 状态窗口可以正常关闭  
✅ 客户收到状态变更通知

### 如果出现问题
❌ 如果订单状态没有自动更新 → 检查 `updateItemStatus` 中的同步逻辑  
❌ 如果通知没有发送 → 检查 `sendCustomerNotification` 函数  
❌ 如果窗口无法关闭 → 检查 `closeOrderDetail` 函数

---

## 测试3：C端下单数据同步

### 测试步骤

1. **创建多物品订单**
   - 打开 C端页面（c-index.html）
   - 选择"皮包护理"（👜 100元）
   - 选择"床单被罩"（🛏️ 60元）
   - 点击下一步

2. **选择门店**
   - 选择"海淀区干洗店"
   - 点击选择该门店（应高亮显示）
   - 点击下一步

3. **选择配送方式**
   - 选择"自送到店"（享受9折）
   - 点击下一步

4. **填写信息并下单**
   - 填写联系人、电话、地址
   - 点击"确认下单"

5. **完成支付**
   - 选择支付方式
   - 点击"确认支付"
   - 等待支付完成

6. **验证数据同步**
   - 支付成功后，立即打开 m-index.html
   - 等待8秒或手动刷新
   - 检查是否看到新订单
   - 点击查看订单详情
   - 验证物品清单显示"皮包护理"和"床单被罩"
   - 验证门店信息为"海淀区干洗店"

7. **切换门店验证**
   - 在 m-index 中切换到"朝阳区干洗店"
   - 确认海淀区店的订单不在列表中显示
   - 切换回"海淀区干洗店"
   - 确认订单仍然存在

### 预期结果
✅ C端下单的物品信息正确传递到门店端  
✅ 门店ID正确关联，订单只出现在对应门店  
✅ 切换门店账号时，订单列表正确过滤  
✅ 自送到店和跑腿配送都能正常完成支付  
✅ 订单状态流程正确（待处理 → 清洗中 → 清洗完成 → 待取件 → 已完成）

### 如果出现问题
❌ 如果门店选择无法确认 → 检查 `selectStore` 函数  
❌ 如果支付无法确认 → 检查 `confirmOrder` 中的验证逻辑  
❌ 如果物品显示错误 → 检查 `confirmOrder` 中 `items` 字段的设置  
❌ 如果数据没有同步 → 检查 `syncC端Orders` 和支付成功后的同步代码

---

## 附加测试：跨标签页同步

### 测试步骤

1. 打开两个浏览器标签页，都访问 m-index.html
2. 在第一个标签页中更新某个物品的状态
3. 观察第二个标签页是否自动刷新订单列表
4. 在C端下一个新订单
5. 观察两个门店端标签页是否都能看到新订单

### 预期结果
✅ 跨标签页数据同步正常工作  
✅ 状态栏显示同步状态（可选）

---

## 问题排查清单

### 问题：切换账号后订单列表为空
**可能原因**：
- 该门店确实没有订单
- 订单的 `storeId` 字段设置不正确
- 过滤逻辑有问题

**解决方法**：
1. 检查 localStorage 中的 `store_orders` 数据
2. 确认订单的 `storeId` 与当前门店匹配
3. 在浏览器控制台查看过滤日志

### 问题：物品状态更新后订单状态没有同步
**可能原因**：
- `updateItemStatus` 函数的同步逻辑被跳过
- 订单状态已经是目标状态
- 物品数组为空

**解决方法**：
1. 检查浏览器控制台是否有错误
2. 确认订单包含 `items` 数组
3. 查看 `updateItemStatus` 的同步逻辑

### 问题：C端下单后门店端看不到订单
**可能原因**：
- 门店ID不匹配
- 同步函数没有执行
- localStorage 数据问题

**解决方法**：
1. 在C端支付成功后检查 `store_orders` 数据
2. 确认 `syncC端Orders` 函数正在运行
3. 手动刷新门店端页面

---

## 测试完成确认

完成所有测试后，请在下方打勾：

- [ ] 测试1：账号切换与订单过滤 ✅
- [ ] 测试2：物品状态自动同步订单状态 ✅
- [ ] 测试3：C端下单数据同步 ✅
- [ ] 附加测试：跨标签页同步 ✅

如果有任何测试失败，请在问题排查清单中查找对应的解决方案，或联系开发人员获取帮助。

---

## 联系与反馈

如有疑问或发现问题，请：
1. 查看浏览器控制台（Console）的错误信息
2. 检查 localStorage 中的数据
3. 参考 `ORDER-FEATURE-FIX-20260426.md` 中的技术细节
4. 联系开发团队
