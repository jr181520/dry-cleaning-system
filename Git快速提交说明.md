# Git 快速提交工具使用说明

## 📦 工具简介

提供了两个快速提交脚本，方便你快速保存代码修改：
1. `Git快速提交.bat` - Windows批处理版本
2. `Git快速提交.ps1` - PowerShell版本（推荐）

## 🚀 使用方法

### 方法1：双击运行（推荐）

1. **双击** `Git快速提交.ps1` 文件
2. 按提示操作：
   - 检测到修改 → 输入 `Y` 确认
   - 输入提交信息 → 直接回车使用默认，或输入自定义信息
   - 完成后选择是否推送到远程

### 方法2：右键菜单

1. 右键点击 `Git快速提交.ps1`
2. 选择"使用 PowerShell 运行"

### 方法3：在终端运行

```bash
# 在项目目录打开终端
powershell -File Git快速提交.ps1
```

## ✨ 功能特性

- ✅ 自动检测所有修改的文件
- ✅ 预览修改内容
- ✅ 支持自定义提交信息
- ✅ 可选推送到远程仓库
- ✅ 中文界面，操作简单

## 📋 操作流程

```
运行脚本
    ↓
检测修改文件
    ↓
输入 Y 确认
    ↓
输入提交信息（可选）
    ↓
自动 git add + git commit
    ↓
选择是否 git push
```

## ⚠️ 注意事项

1. **首次使用**：确保已安装Git
2. **中文路径**：脚本已优化，支持中文路径
3. **提交信息**：建议输入有意义的描述，方便日后查找

## 🎯 最佳实践

### 场景1：修复bug后
```
[修复] 修复了订单列表显示空白的问题
```

### 场景2：添加新功能
```
[新增] 添加了代客下单功能
```

### 场景3：优化代码
```
[优化] 重构了订单同步逻辑
```

## 🔧 如果脚本无法运行

检查PowerShell执行策略：
```powershell
# 查看当前策略
Get-ExecutionPolicy

# 如果是Restricted，改为RemoteSigned
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📞 常见问题

**Q: 提示"禁止运行脚本"？**
A: 以管理员身份运行PowerShell，执行：`Set-ExecutionPolicy RemoteSigned`

**Q: 没有检测到修改？**
A: 检查是否真的修改了文件，或者文件是否在.gitignore中

**Q: 提交信息乱码？**
A: 使用PowerShell版本（.ps1），已优化编码支持
