# 数据同步与离线使用指南

## 📋 功能概述

本系统已实现完整的数据同步机制，支持多个页面自动同步订单数据到本地存储，确保您在各种环境下都能顺利测试和使用系统。

## ✅ 已启用同步的页面

### 当前版本（已完成）

1. **C端订单页面** (`c-orders.html`)
   - 同步键：`orders`
   - 功能：用户查看个人订单
   - 状态：✅ 已启用

2. **M端管理页面** (`m-index.html`)
   - 同步键：`store_orders`
   - 功能：门店管理员查看本店订单
   - 状态：✅ 已启用

3. **管理员后台** (`admin.html`)
   - 同步键：`all_orders`
   - 功能：超级管理员查看所有订单
   - 状态：✅ 已启用

### 待开发（未来版本）

- 🔜 微信小程序端数据同步
- 🔜 C端主页面 (`index.html`) 同步
- 🔜 M端管理员页面 (`m-admin.html`) 同步

## 🔄 数据同步机制

### 1. 自动同步（localhost环境）

当您在 **http://localhost:3000** 环境下使用时：

- ✅ 系统自动每5秒从后端API获取最新订单数据
- ✅ 自动合并到本地localStorage
- ✅ 当页面重新可见时（从后台切换回来），自动同步
- ✅ 控制台显示同步日志（可在浏览器开发者工具中查看）

### 2. 本地文件离线使用

当您使用本地文件（双击HTML文件）打开页面时：

- ✅ 自动使用上次同步的localStorage备份数据
- ✅ 即使没有启动后端服务也能查看历史订单
- ✅ 显示"数据备份时间"提示

## 📱 如何使用

### 场景1：日常开发测试（推荐）

1. **启动服务**
   ```bash
   cd d:\Trae CN\bin\dry_cleaning_system
   一键启动.bat
   ```

2. **访问页面**
   - C端订单页面：`http://localhost:3000/c-orders.html`
   - M端管理页面：`http://localhost:3000/m-index.html`

3. **查看同步日志**
   - 按 F12 打开浏览器开发者工具
   - 切换到 Console 标签
   - 看到 `[同步 xx:xx:xx] ℹ️ 开始同步订单数据...` 等日志

### 场景2：离线使用（第二天开机测试）

1. **打开本地文件**
   - 直接双击 `c-orders.html` 或 `m-index.html`
   - 或右键 → 打开方式 → 选择浏览器

2. **自动加载备份数据**
   - 系统自动检测无法连接API
   - 从localStorage读取上次同步的数据
   - 显示备份时间提示

3. **手动触发同步（如需）**
   - 在浏览器控制台执行：
     ```javascript
     OrderSyncManager.manualSync()
     ```

## 🗺️ 地图展示功能

### 查看门店地图

在订单列表中，如果订单包含门店位置信息，会显示 **"🗺️ 查看地图"** 按钮：

1. 点击按钮
2. 弹出模态框显示门店位置
3. 支持缩放、拖拽查看
4. 显示门店名称、地址和坐标

### 数据来源

- 门店坐标从后端数据库获取
- 使用 OpenStreetMap 免费地图
- 无需API密钥

## 🔍 数据同步流程图

```
┌─────────────────────────────────┐
│   localhost 环境 (http://)      │
│                                 │
│   页面加载                      │
│       ↓                         │
│   尝试连接API                  │
│       ↓                         │
│   ┌──────────────────┐         │
│   │ 连接成功          │         │
│   │ • 获取服务器订单  │         │
│   │ • 保存到localStorage│       │
│   │ • 每5秒自动同步   │         │
│   └──────────────────┘         │
│       ↓                         │
│   页面显示订单                  │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│   本地文件 (file://)            │
│                                 │
│   页面加载                      │
│       ↓                         │
│   尝试连接API                  │
│       ↓                         │
│   ┌──────────────────┐         │
│   │ 连接失败          │         │
│   │ • 读取localStorage│         │
│   │ • 显示备份数据    │         │
│   │ • 提示备份时间    │         │
│   └──────────────────┘         │
│       ↓                         │
│   页面显示历史订单              │
└─────────────────────────────────┘
```

## 💡 提示与技巧

### 1. 强制刷新同步

如果发现数据不同步：

```javascript
// 在浏览器控制台执行
OrderSyncManager.manualSync()
```

### 2. 查看同步状态

```javascript
OrderSyncManager.getStatus()
```

### 3. 停止自动同步（调试用）

```javascript
OrderSyncManager.stop()
```

### 4. 查看备份数据

```javascript
// 查看备份的订单
JSON.parse(localStorage.getItem('orders_backup'))

// 查看备份时间
localStorage.getItem('orders_backup_time')
```

### 5. 清除所有同步数据

```javascript
localStorage.removeItem('orders_backup')
localStorage.removeItem('orders_backup_time')
```

## 🔧 故障排除

### 问题1：本地文件打开看不到订单

**原因**：从未在localhost环境下同步过数据

**解决**：
1. 先启动后端服务
2. 在localhost环境下打开页面
3. 等待数据同步完成
4. 然后才能在本地文件模式下查看备份

### 问题2：门店地图不显示

**检查项**：
1. 确认订单有门店位置信息
2. 检查浏览器控制台是否有错误
3. 确认网络正常（地图需要加载OSM瓦片）

### 问题3：同步一直失败

**可能原因**：
- 后端服务未启动
- 端口被占用
- 网络问题

**解决**：
1. 重启后端服务
2. 检查端口3000是否被占用
3. 查看浏览器控制台错误信息

## 📊 数据存储位置

| 数据类型 | localStorage键 | 说明 |
|---------|---------------|------|
| C端订单 | `orders` | 当前C端订单列表 |
| C端备份 | `orders_backup` | API同步的备份 |
| M端订单 | `store_orders` | M端门店订单 |
| M端备份 | `store_orders_backup` | M端订单备份 |
| 备份时间 | `orders_backup_time` | 最后同步时间 |

## 🎯 最佳实践

1. **每天首次使用时**
   - 先启动localhost服务
   - 打开页面自动同步
   - 然后可以关闭服务，用本地文件查看

2. **开发新功能时**
   - 始终使用localhost环境
   - 查看控制台日志确认同步正常
   - 测试完成后关闭服务

3. **离线演示时**
   - 提前同步好数据
   - 使用无痕/隐私模式打开本地文件
   - 避免缓存干扰

## 📞 技术支持

如遇问题：
1. 查看浏览器控制台错误信息
2. 检查后端服务是否正常运行
3. 查看Git提交记录了解最新改动

---

**更新时间**: 2026-05-05  
**版本**: v2.0
