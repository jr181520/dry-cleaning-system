# 订单同步测试指南

## 测试目的
验证 C端小程序、门店端和管理后台三个系统之间的订单数据同步功能。

## 测试工具
文件：`test-order-sync.html`

## 测试步骤

### 1️⃣ 打开测试工具
双击打开 `test-order-sync.html` 文件

### 2️⃣ 填写订单信息
- **用户信息**：输入用户名和手机号
- **服务项目**：勾选要下单的服务（至少选1项）
- **选择门店**：选择目标门店（ST001/ST002/ST003）
- **配送方式**：
  - 到店自取：不收配送费
  - 配送到家：加收 ¥10 配送费

### 3️⃣ 执行模拟下单
点击 **"🚀 模拟下单"** 按钮

### 4️⃣ 观察测试日志
- ✅ 检查日志输出，确认订单创建成功
- ✅ 查看订单号
- ✅ 确认数据保存位置

### 5️⃣ 验证三个系统

#### 📱 C端小程序
1. 打开 `c-orders.html` 页面
2. 在侧边栏点击 "我的订单" 或 "订单"
3. 查看是否显示刚才创建的订单

#### 🏪 门店端
1. 打开 `m-index.html` 页面
2. 进入"订单管理"菜单
3. 查看是否显示该门店的订单

#### 💼 管理后台
1. 打开 `admin.html` 页面
2. 进入"订单管理"菜单
3. 查看是否显示所有订单

### 6️⃣ 使用快速测试
点击快速测试按钮可以快速创建预设的测试订单

## 当前实现情况

### ✅ 已实现
- C端下单保存到 localStorage
- 后端 API 接口已定义
- 门店端从后端 API 获取订单
- 管理后台从后端 API 获取订单

### ⚠️ 待完善
- C端下单暂未调用后端 API
- 订单数据未实时同步到数据库
- 需要后端服务运行才能完整测试

## 测试场景

### 场景1：纯前端测试（后端未运行）
```bash
1. 点击"模拟下单"
2. 日志显示："⚠️ 后端 API 暂时不可用"
3. 订单保存到 localStorage
4. 打开 C端小程序 c-orders.html 可以看到订单
5. 门店端和管理后台看不到订单（因为需要后端）
```

### 场景2：完整测试（后端运行）
```bash
1. 确保后端服务运行在 localhost:3000
2. 点击"模拟下单"
3. 日志显示："✅ 后端 API 创建成功"
4. 三个系统都能看到订单
```

## 查看订单数据

### localStorage (C端)
打开浏览器开发者工具（F12） → Application → Local Storage → 查看 `orders` 键

### 后端数据库 (MongoDB)
需要连接 MongoDB 查看 `orders` 集合

## 故障排除

### 问题1：订单创建成功但各系统看不到
**原因**：后端 API 未运行
**解决**：启动后端服务
```bash
cd backend
npm start
```

### 问题2：C端看不到订单
**原因**：localStorage 未同步
**解决**：刷新 c-orders.html 页面

### 问题3：门店端看不到订单
**原因**：门店 ID 不匹配或 API 调用失败
**解决**：
1. 检查控制台错误
2. 确认门店端登录的门店 ID
3. 查看 localStorage 中的 `store_orders`

### 问题4：管理后台看不到订单
**原因**：未登录或 API 调用失败
**解决**：
1. 确认已登录管理后台
2. 检查控制台网络请求
3. 查看浏览器 Network 面板的 API 响应

## 下一步优化

### 优化1：实现 C端下单实时同步到后端
在 `c-order.html` 的 `confirmOrder()` 函数中添加：
```javascript
// 发送到后端
try {
    const response = await fetch('http://localhost:3000/api/admin/orders', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(order)
    });
} catch (e) {
    console.error('同步失败:', e);
}
```

### 优化2：添加 WebSocket 实时推送
实现订单状态变更实时通知到各端

### 优化3：完善订单状态流转
定义完整的状态机：pending → paid → processing → ready → completed

## 技术架构

```
┌─────────────────┐
│  C端小程序      │
│ c-order.html   │
└────────┬────────┘
         │ localStorage
         │ + 后端 API (待实现)
         ▼
┌─────────────────┐
│  后端服务       │
│ Node.js +      │
│ MongoDB        │
└────────┬────────┘
         │ REST API
    ┌────┴────┐
    ▼         ▼
┌────────┐  ┌────────┐
│ 门店端 │  │ 管理后台│
│ m-index│  │ admin  │
└────────┘  └────────┘
```

## 注意事项

⚠️ **重要提示**：
1. 测试时请使用不同的订单号，避免重复
2. 每次刷新页面后订单数据会重新加载
3. localStorage 有大小限制（约5MB）
4. 实际生产环境需要实现用户认证和权限控制

---

**测试愉快！** 🎉

如有疑问，请查看控制台日志或联系开发人员。
