# 数据同步功能测试清单

## ✅ 已完成的数据同步页面

### 1. C端订单页面 (c-orders.html)
**访问地址**: `http://localhost:3000/c-orders.html`  
**同步键**: `orders`  
**测试步骤**:
- [ ] 打开页面
- [ ] 按 F12 查看控制台日志
- [ ] 确认看到 `[同步]` 日志
- [ ] 创建新订单
- [ ] 关闭浏览器，重新打开本地文件 `c-orders.html`
- [ ] 确认订单数据仍在

### 2. M端管理页面 (m-index.html)
**访问地址**: `http://localhost:3000/m-index.html`  
**同步键**: `store_orders`  
**测试步骤**:
- [ ] 打开页面并登录
- [ ] 按 F12 查看控制台日志
- [ ] 确认看到 `[同步]` 日志
- [ ] 创建代客下单
- [ ] 查看订单列表是否更新
- [ ] 关闭服务，打开本地文件 `m-index.html`
- [ ] 确认门店订单数据仍在

### 3. 管理员后台 (admin.html)
**访问地址**: `http://localhost:3000/admin.html`  
**同步键**: `all_orders`  
**测试步骤**:
- [ ] 打开页面并登录
- [ ] 按 F12 查看控制台日志
- [ ] 确认看到 `[同步]` 日志
- [ ] 查看所有订单数据
- [ ] 关闭服务，打开本地文件 `admin.html`
- [ ] 确认管理员订单数据仍在

## 🔍 验证同步功能

### 方法1：控制台日志
按 F12 打开开发者工具，切换到 Console 标签：

```javascript
// 应该看到类似日志：
[同步 22:18:30] ℹ️ 开始同步订单数据...
[同步 22:18:30] ℹ️ 从 /cleaning/orders 获取到 17 条订单
[同步 22:18:30] ℹ️ 合并了 X 条新订单到本地
[同步 22:18:30] ℹ️ 同步完成
```

### 方法2：localStorage检查
在控制台执行：

```javascript
// 查看各页面的备份数据
console.log('C端订单备份:', localStorage.getItem('orders_backup'))
console.log('M端订单备份:', localStorage.getItem('store_orders_backup'))
console.log('管理员订单备份:', localStorage.getItem('all_orders_backup'))
console.log('备份时间:', localStorage.getItem('orders_backup_time'))
```

### 方法3：手动同步
在控制台执行：

```javascript
OrderSyncManager.manualSync()
```

## 📱 离线使用测试

### 测试场景：第二天开机

1. **第一天**
   - 启动服务：`一键启动.bat`
   - 访问各页面进行测试
   - 确认数据同步正常

2. **第二天（不启动服务）**
   - 直接双击打开 `c-orders.html`
   - 应该自动加载备份数据
   - 查看控制台是否有提示：
     ```
     API获取失败，使用localStorage备份数据，共 X 条订单
     数据备份时间: 2026/5/5 xx:xx:xx
     ```

3. **验证备份完整性**
   - 打开浏览器开发者工具
   - 执行：`JSON.parse(localStorage.getItem('orders_backup')).length`
   - 应该返回订单数量

## 🗺️ 地图功能测试

### 测试门店地图显示

1. **准备测试数据**
   ```bash
   # 确保有一个带门店信息的订单
   curl "http://localhost:3000/api/cleaning/orders?page=1&pageSize=1"
   # 确认返回的订单有 store.location.coordinates 字段
   ```

2. **测试地图按钮**
   - 访问 `http://localhost:3000/c-orders.html`
   - 找到有门店信息的订单卡片
   - 应该看到 "🗺️ 查看地图" 按钮

3. **测试地图模态框**
   - 点击 "🗺️ 查看地图" 按钮
   - 应该弹出模态框
   - 显示地图和门店标记
   - 显示门店名称、地址、坐标

4. **验证地图功能**
   - [ ] 地图正确加载
   - [ ] 门店标记位置正确（北京中关村：39.9835, 116.3074）
   - [ ] 点击标记显示信息窗口
   - [ ] 可以缩放和拖拽
   - [ ] 点击关闭按钮或背景可以关闭模态框

## 🎯 常见问题排查

### 问题1：看不到同步日志
**检查项**:
- [ ] 浏览器控制台是否打开（F12）
- [ ] Console 标签是否选中
- [ ] 网络请求是否成功（Network 标签）
- [ ] 后端服务是否运行

### 问题2：localStorage备份为空
**原因**: 从未在localhost环境下同步过数据

**解决**:
1. 先启动服务
2. 访问各页面
3. 等待自动同步完成（看到 `[同步]` 日志）
4. 然后才能在本地文件模式下使用备份

### 问题3：地图按钮不显示
**检查项**:
- [ ] 订单是否有门店信息（store字段）
- [ ] 门店是否有位置坐标（location字段）
- [ ] 浏览器控制台是否有错误
- [ ] 刷新页面重试

### 问题4：地图加载失败
**可能原因**:
- 网络问题（无法加载OSM瓦片）
- Leaflet库加载失败

**解决**:
1. 检查网络连接
2. 刷新页面重试
3. 查看控制台错误信息

## 📊 数据流向图

```
┌─────────────────────────────────────────────────────┐
│           数据同步流程                              │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐     ┌──────────────┐            │
│  │  localhost   │ ──→ │  后端API     │            │
│  │  各页面      │     │  /api/orders │            │
│  └──────────────┘     └──────────────┘            │
│         │                       │                  │
│         │ 同步订单               │ 返回订单数据     │
│         ↓                       ↓                  │
│  ┌──────────────────────────────────────┐         │
│  │         localStorage                  │         │
│  │  ├─ orders_backup (C端)               │         │
│  │  ├─ store_orders_backup (M端)         │         │
│  │  └─ all_orders_backup (管理员)         │         │
│  └──────────────────────────────────────┘         │
│         │                                          │
│         │ 本地文件模式读取备份                       │
│         ↓                                          │
│  ┌──────────────┐                                 │
│  │  file://     │                                 │
│  │  各页面      │                                 │
│  └──────────────┘                                 │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## 📝 测试记录模板

### 页面测试记录

**测试页面**: _______________  
**测试时间**: _______________  
**测试人员**: _______________  

#### 功能测试
- [ ] 页面正常加载
- [ ] 控制台显示同步日志
- [ ] 数据成功同步到localStorage
- [ ] 关闭服务后本地文件仍能查看数据

#### 地图功能（仅c-orders.html）
- [ ] 门店地图按钮显示
- [ ] 地图模态框正常弹出
- [ ] 地图瓦片正确加载
- [ ] 门店标记位置正确

#### 问题记录
```
问题描述：_______________________________
复现步骤：_______________________________
控制台错误：_____________________________
```

## 🎉 测试完成确认

完成所有测试后，请确认：

- [ ] ✅ c-orders.html 同步正常
- [ ] ✅ m-index.html 同步正常
- [ ] ✅ admin.html 同步正常
- [ ] ✅ 地图功能正常显示
- [ ] ✅ 离线模式可查看备份数据
- [ ] ✅ 无重大问题

**测试总结**: _______________________________  
**提交版本**: _______________

---

**更新时间**: 2026-05-05  
**版本**: v2.0
