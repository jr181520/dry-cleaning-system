# 代客下单支付系统 - 快速测试指南

## 测试前的准备工作

### 1. 启动支付服务器

```bash
cd d:\Trae CN\bin\dry_cleaning_system\api\payment-server
node server.js
```

服务器启动后应显示：
```
========================================
  支付网关服务器已启动
  端口: http://localhost:3002
========================================

支持的支付方式:
  🚗 微信支付 (wechat)
  📱 支付宝 (alipay)
  💳 银联支付 (unionpay)
  💰 账户余额 (balance)
  🏪 POS支付 (pos)
  💳 会员卡 (member-card)
```

### 2. 测试账号

#### 会员卡账号

| 卡号 | 类型 | 余额 | 密码 |
|------|------|------|------|
| SYS_001 | 系统会员卡 | ¥500.00 | 123456 |
| STO_001_ST001 | 商家专属卡 | ¥200.00 | 123456 |

## 测试流程

### 测试1：会员卡充值（C端）

1. 打开 `c-recharge.html`
2. 查看当前会员卡余额（应为 ¥500.00）
3. 选择充值金额：¥1000
4. 点击"立即充值"
5. 选择支付方式：微信支付
6. 点击"确认支付"
7. 充值成功后，余额应变为 ¥1600.00

**预期结果**：
- 充值成功弹窗显示
- 赠送金额 ¥100
- 到账金额 ¥1100

### 测试2：代客下单 - 现金支付（C端）

1. 打开 `c-order.html`
2. 选择服务项目（如：西装干洗 ¥50）
3. 选择门店
4. 选择"自送到店"
5. 点击"下一步"
6. 填写联系方式
7. 点击"确认下单"
8. 在支付页面选择 **"现金支付"**
9. 点击"生成付款码"
10. 查看生成的二维码

**预期结果**：
- 订单创建成功
- 支付页面显示现金支付说明
- 点击"生成付款码"后显示二维码弹窗

### 测试3：代客下单 - 会员卡支付（C端）

1. 在支付页面选择 **"会员卡支付"**
2. 选择会员卡（SYS_001）
3. 确认卡余额充足
4. 点击"确认支付"

**预期结果**：
- 显示会员卡选择弹窗
- 扣除会员卡余额
- 订单支付成功

### 测试4：POS收银（门店端）

1. 打开 `m-pos.html`
2. 输入收款金额：¥88.88
3. 点击数字键输入自定义金额

#### 场景A：扫码支付
1. 选择"扫码收款"
2. 点击"模拟扫码（测试用）"
3. 等待支付成功

**预期结果**：
- 显示扫码弹窗
- 点击模拟扫码后交易成功
- 显示成功弹窗
- 交易记录更新

#### 场景B：现金支付
1. 选择"现金收款"
2. 点击"确认收款 ¥88.88"
3. 查看找零弹窗（找零 ¥0.00）
4. 点击"完成交易"

**预期结果**：
- 显示找零弹窗
- 点击完成交易后交易成功
- 交易记录更新

#### 场景C：会员卡支付
1. 输入收款金额
2. 选择"会员卡"
3. 输入卡号：SYS_001
4. 输入密码：123456
5. 点击"验证会员卡"

**预期结果**：
- 验证成功后扣除会员卡余额
- 交易成功
- 交易记录更新

### 测试5：会员卡余额查询

1. 打开 `c-profile.html`
2. 查看会员卡余额
3. 打开 `c-recharge.html`
4. 查看会员卡信息

**预期结果**：
- 显示当前余额
- 显示卡类型（系统卡/商家卡）

## API接口测试

### 测试POS API

```bash
# 创建POS订单
curl -X POST http://localhost:3002/api/pos/create \
  -H "Content-Type: application/json" \
  -d '{
    "orderId": "TEST001",
    "storeId": "ST001",
    "amount": 100,
    "customerPhone": "13800138001"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "data": {
    "orderId": "TEST001",
    "paymentId": "POS_xxx",
    "amount": 100,
    "qrCode": "https://pay.drycleaning-system.com/qr/POS_xxx",
    "status": "pending"
  }
}
```

### 测试会员卡API

```bash
# 查询会员卡
curl http://localhost:3002/api/member-card/info/SYS_001

# 查询用户所有会员卡
curl http://localhost:3002/api/member-card/user/user_001

# 充值会员卡
curl -X POST http://localhost:3002/api/member-card/recharge \
  -H "Content-Type: application/json" \
  -d '{
    "cardId": "SYS_001",
    "amount": 200,
    "userId": "user_001"
  }'
```

### 测试支付接口

```bash
# 创建支付订单
curl -X POST http://localhost:3002/api/payment/create \
  -H "Content-Type: application/json" \
  -d '{
    "orderId": "TEST002",
    "amount": 88.88,
    "paymentMethod": "balance",
    "userId": "user_001"
  }'
```

## 常见问题排查

### 问题1：支付服务器无法启动

**症状**：运行 `node server.js` 报错

**解决方案**：
1. 检查端口 3002 是否被占用
```bash
netstat -ano | findstr :3002
```

2. 如果端口被占用，修改 `config.js` 中的端口

### 问题2：C端无法连接支付服务器

**症状**：页面显示"获取会员卡失败"或无响应

**解决方案**：
1. 确认支付服务器已启动
2. 检查浏览器控制台错误信息
3. 确认 `c-payment.html` 和 `c-recharge.html` 中的 API 地址正确

### 问题3：会员卡支付失败

**症状**：点击会员卡支付无反应

**解决方案**：
1. 检查网络连接
2. 确认会员卡余额充足
3. 检查 API 服务器日志

### 问题4：二维码无法生成

**症状**：点击生成付款码无响应

**解决方案**：
1. 检查 `qrcode-generator` 库是否正确加载
2. 查看浏览器控制台错误
3. 模拟支付使用测试数据

## 功能清单

### ✅ 已实现功能

- [x] 现金支付（POS收银系统）
- [x] 会员卡支付（C端余额支付）
- [x] 订单二维码生成
- [x] 扫码支付（POS端）
- [x] 现金找零计算
- [x] 会员卡充值（C端）
- [x] 会员卡余额查询
- [x] 商家专属会员卡支持
- [x] 系统通用会员卡支持
- [x] 交易记录保存
- [x] 结算中心同步
- [x] 小票数据生成
- [x] 多种支付方式集成

### 🔧 预留接口

- [ ] 真实POS设备对接
- [ ] 商家会员系统API对接
- [ ] 小票打印机对接
- [ ] 真实微信/支付宝支付

## 技术文档

详细的API文档和功能说明请查看：
- `代客下单支付系统功能说明.md`

## 联系与支持

如有问题，请检查：
1. 支付服务器日志
2. 浏览器开发者工具控制台
3. Network 标签页的网络请求

祝测试顺利！🎉
