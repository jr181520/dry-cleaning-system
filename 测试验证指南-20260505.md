# 问题修复验证指南
## 日期：2026年5月5日

### 1. M端登录提示问题

**问题描述**：
- 在M端登录后，反复出现"检测到您尚未登录门店账号，是否前往登录？"提示
- 取消后登录信息被保留，但下次打开又出现

**修复方案**：
- ✅ 引入访客模式标记（guestModeHandled）
- ✅ 用户取消登录后不再重复提示
- ✅ 登录成功或切换账号时清除标记

**验证步骤**：
1. 打开浏览器访问：http://localhost:3000/m-index.html
2. 如果出现登录提示，点击"取消"
3. 刷新页面（F5），验证是否还会弹出提示
4. 再次打开登录页面，重新登录ST002门店
5. 刷新m-index.html，验证门店登录信息是否正常

---

### 2. C端门店信息显示问题

**问题描述**：
- C端订单列表显示"默认门店/门店地址空白/undefined"
- 历史订单的门店信息也缺失

**修复方案**：
- ✅ 修复门店查询逻辑：使用storeNo而不是_id匹配
- ✅ 添加Store模型定义到orderService
- ✅ 修复启动脚本路径问题

**验证步骤**：
1. 确保服务器已重启（使用"重启服务器.bat"）
2. 打开浏览器访问：http://localhost:3000/c-orders.html
3. 查看订单列表，验证门店信息是否正确显示：
   - ✅ 门店名称（如"干洗连锁-北京中关村店"）
   - ✅ 门店地址（如"北京市海淀区中关村大街1号"）
4. 点击任意订单查看详情，验证门店信息是否完整

---

### 3. 服务器端口合并问题

**问题描述**：
- "启动统一服务.bat"报错找不到package.json
- npm install在错误目录执行

**修复方案**：
- ✅ 修改启动脚本，先cd到backend目录
- ✅ 依赖安装在正确位置

**验证步骤**：
1. 双击"启动统一服务.bat"
2. 验证是否显示"后端依赖已安装"或开始安装依赖
3. 验证服务是否成功启动在localhost:3000

---

## 快速测试脚本

### 测试1：M端登录检测
```javascript
// 在浏览器控制台执行
console.log('storeUser:', localStorage.getItem('storeUser'));
console.log('guestModeHandled:', localStorage.getItem('guestModeHandled'));
console.log('currentStore:', localStorage.getItem('currentStore'));
```

### 测试2：C端门店信息
访问 http://localhost:3000/api/cleaning/orders?page=1&pageSize=5
查看返回的JSON数据中是否包含：
- `storeName`
- `storeAddress`
- `store` 对象（包含完整门店信息）

---

## 常见问题排查

### 问题1：重启服务器后问题依旧
**解决**：确保所有修改已保存，并完整重启服务器

### 问题2：门店信息仍然显示undefined
**解决**：
1. 检查MongoDB中是否有门店数据：`node backend/show-stores.js`
2. 检查订单数据中的storeId格式
3. 再次重启服务器

### 问题3：登录提示仍然出现
**解决**：
1. 清除浏览器缓存或打开无痕模式
2. 执行登录提示中的"是"跳转到登录页面
3. 重新登录
