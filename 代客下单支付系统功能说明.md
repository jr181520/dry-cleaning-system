# 代客下单支付系统功能说明

## 功能概述

本次实现为代客下单功能添加了两种新的支付方式：

1. **现金支付** - 支持POS系统收银和订单二维码扫码支付
2. **会员卡余额支付** - 支持C端会员卡充值和余额支付

## 功能架构

### 1. 后端API

#### POS系统API (`api/payment-server/pos-api.js`)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/pos/create` | POST | 创建POS订单，生成支付二维码 |
| `/api/pos/query/:orderId` | GET | 查询POS订单状态 |
| `/api/pos/scan` | POST | POS扫码支付 |
| `/api/pos/cash` | POST | 现金支付 |
| `/api/pos/card` | POST | 会员卡支付 |
| `/api/pos/callback` | POST | 支付回调 |
| `/api/pos/receipt/:orderId` | GET | 获取小票数据 |
| `/api/pos/settlement/sync` | POST | 同步到结算中心 |

#### 会员卡API (`api/payment-server/member-card.js` 和 `member-card-routes.js`)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/member-card/create` | POST | 创建会员卡 |
| `/api/member-card/info/:cardId` | GET | 查询会员卡信息 |
| `/api/member-card/user/:userId` | GET | 查询用户的所有会员卡 |
| `/api/member-card/verify` | POST | 验证并扣除会员卡余额 |
| `/api/member-card/recharge` | POST | 会员卡充值 |
| `/api/member-card/transactions/:cardId` | GET | 获取交易记录 |
| `/api/member-card/verify-password` | POST | 验证密码 |
| `/api/member-card/change-password` | POST | 修改密码 |
| `/api/member-card/toggle-status` | POST | 挂失/解挂会员卡 |
| `/api/member-card/store/:storeId` | GET | 查询门店可用会员卡 |

### 2. 会员卡系统

#### C端自有系统会员卡
- **特点**：可在平台所有商家使用
- **卡号格式**：`SYS_xxxxxxxx`
- **权益**：
  - 全场95折优惠
  - 充值赠送（500元送5%，1000元送10%，2000元送15%）
  - 2倍积分奖励
  - 免费配送服务

#### 商家专属会员卡
- **特点**：只能在指定商家使用
- **卡号格式**：`STO_xxxxxxxx_STxxx`
- **权益**：由各商家自定义设置
- **适用场景**：成熟商家需要保留专属会员管理系统

#### 会员卡类型对比

| 特性 | C端系统会员卡 | 商家专属会员卡 |
|------|--------------|--------------|
| 使用范围 | 所有商家 | 指定商家 |
| 卡号前缀 | SYS_ | STO_ |
| 余额管理 | 平台统一 | 商家独立 |
| API预留 | 是 | 是 |
| 商家接入 | N/A | 预留API接口 |

### 3. 现金支付流程

#### 场景1：门店收银
1. 顾客到店，选择现金支付
2. 门店使用POS收银系统（`m-pos.html`）
3. 输入收款金额
4. 选择支付方式：
   - **扫码支付**：扫描用户微信/支付宝付款码
   - **现金支付**：输入收款金额，计算找零
   - **会员卡支付**：验证会员卡，扣除余额
   - **二维码支付**：生成收款二维码，用户扫码支付
5. 交易完成，自动打印小票

#### 场景2：代客下单
1. 代客人员在C端创建订单（`c-order.html`）
2. 选择现金支付方式
3. 系统生成订单专属付款二维码
4. 顾客扫码支付
5. 资金入账系统，同步到结算中心

### 4. 支付方式集成

#### C端支付页面 (`c-payment.html`)

新增支付方式：
- 💳 **会员卡支付** - 使用会员卡余额
- 💵 **现金支付** - 生成付款码，支持扫码

#### M端POS收银系统 (`m-pos.html`)

功能：
- 数字键盘输入收款金额
- 快捷金额按钮（50/100/200/500元）
- 多种支付方式选择
- 扫码枪接口预留
- 找零计算
- 小票打印接口预留
- 交易记录查询

### 5. 与结算中心打通

POS系统与结算中心的对接：

```
POS收银 → API调用 → 结算中心同步
                ↓
        资金自动分账
                ↓
        商家提现功能
```

**同步数据**：
- 订单号
- 门店ID
- 支付金额
- 支付方式
- 交易时间
- 交易流水号

## 预留接口

### 1. POS机API接口

```javascript
// POS设备对接接口
POST /api/pos/device/scan
- 参数：扫描的付款码
- 返回：支付结果

POST /api/pos/device/print
- 参数：小票数据
- 返回：打印状态

POST /api/pos/device/cashbox
- 返回：钱箱状态
```

### 2. 商家会员系统API

```javascript
// 商家会员系统对接接口
POST /api/pos/merchant-card/verify
- 参数：商家卡号、密码
- 返回：验证结果和余额

POST /api/pos/merchant-card/deduct
- 参数：商家卡号、扣款金额
- 返回：扣款结果和新余额
```

## 使用说明

### 启动支付服务器

```bash
cd api/payment-server
node server.js
```

服务器将监听端口 3002，提供以下服务：
- 支付网关（微信、支付宝、银联、余额）
- POS收银系统
- 会员卡系统
- 结算中心

### C端操作流程

1. **代客下单**
   - 打开 `c-order.html`
   - 选择服务项目和门店
   - 填写客户信息
   - 选择现金支付或会员卡支付

2. **会员充值**
   - 打开 `c-recharge.html`
   - 选择充值金额或自定义
   - 选择支付方式（微信/支付宝）
   - 完成充值

3. **支付页面**
   - 打开 `c-payment.html`
   - 选择支付方式
   - 现金支付：生成付款二维码
   - 会员卡支付：选择/验证会员卡

### M端操作流程

1. **POS收银**
   - 打开 `m-pos.html`
   - 输入收款金额
   - 选择支付方式
   - 完成收款

2. **门店管理**
   - 查看交易记录
   - 打印小票
   - 核销订单

## 注意事项

1. **安全建议**
   - 会员卡密码应加密传输和存储
   - POS设备应定期校准
   - 大额交易建议人工确认

2. **性能考虑**
   - 二维码有效期5分钟
   - 支付状态轮询间隔1秒
   - 交易记录本地缓存

3. **扩展建议**
   - 对接真实微信/支付宝支付接口
   - 接入商家现有会员系统
   - 添加打印小票功能
   - 支持电子发票

## 测试账号

### 测试会员卡

| 卡号 | 类型 | 余额 | 密码 |
|------|------|------|------|
| SYS_001 | 系统卡 | ¥500.00 | 123456 |
| STO_001_ST001 | 商家卡 | ¥200.00 | 123456 |

## 下一步

1. 对接真实微信支付商户号
2. 接入商家现有会员系统API
3. 添加小票打印功能
4. 开发PC端收银软件
5. 添加电子发票功能
