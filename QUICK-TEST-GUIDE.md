# 订单功能快速测试指南

**测试时间**: ~15分钟  
**前提条件**: 浏览器打开测试文件

---

## 🧪 测试1: admin.html 订单管理

### 步骤：

1. **打开文件**：`admin.html`

2. **自动登录**：页面会自动创建一个总管理员账号

3. **导航到订单管理**：
   - 点击侧边栏 "业务管理"（会展开子菜单）
   - 点击 "订单管理"

4. **验证功能**：
   - ✅ 查看4个统计卡片（全部、待处理、清洗中、已完成）
   - ✅ 测试状态筛选（下拉选择不同状态）
   - ✅ 测试门店筛选（下拉选择门店）
   - ✅ 测试搜索框（输入订单号或客户名）
   - ✅ 点击 "刷新" 按钮
   - ✅ 如果有订单，点击 "查看" 查看详情弹窗

5. **预期结果**：
   - 统计卡片显示正确的数量
   - 筛选和搜索正常工作
   - 订单列表显示订单信息
   - 详情弹窗显示完整订单信息

**预计时间**: 3分钟

---

## 🧪 测试2: m-admin.html 账号切换

### 步骤：

1. **打开文件**：`m-admin.html`

2. **自动登录**：页面会自动创建一个管理员账号

3. **打开侧边栏**：
   - 点击左上角的菜单按钮（三条横线图标）

4. **找到账号管理区**：
   - 向下滑动侧边栏到底部
   - 找到 "切换账号" 和 "退出登录" 按钮

5. **测试切换账号**：
   - 点击 "切换账号"
   - 在弹窗中选择不同的角色（例如：区域管理员）
   - 点击确认
   - 观察页面刷新，导航菜单项发生变化

6. **测试退出登录**：
   - 再次打开侧边栏
   - 点击 "退出登录"
   - 确认弹出提示
   - 应该跳转到登录页面

7. **预期结果**：
   - 切换账号后页面刷新
   - 不同角色看到不同的导航菜单
   - 退出登录成功跳转到登录页

**预计时间**: 3分钟

---

## 🧪 测试3: m-index.html 完整订单流程

### 步骤：

#### 准备工作（创建测试订单）：

1. **打开测试工具**：`test-full-simulation.html`

2. **创建订单**：
   - 点击 "执行完整模拟"
   - 或手动点击 "创建订单1" 等按钮

3. **记录订单号**：复制创建的订单号

#### 门店端操作：

4. **打开门店端**：`m-index.html`

5. **登录门店账号**：使用门店账号登录（ST001）

6. **查看待处理订单**：
   - 在订单列表中找到刚才创建的订单
   - 订单状态应该是 "待处理"

7. **按顺序执行订单操作**：
   - **点击 "开始清洗"** → 状态变为 "清洗中"
   - **点击 "清洗完成"** → 状态变为 "清洗完成"
   - **点击 "通知取件"** → 状态变为 "待取件"
   - **点击 "确认取件"** → 状态变为 "已完成"

8. **每一步验证**：
   - 订单状态正确更新
   - 按钮文案变化
   - 订单卡片上的状态标签颜色变化

9. **预期结果**：
   - 所有按钮都可以点击
   - 状态按预期流转
   - 没有错误提示

**预计时间**: 5分钟

---

## 🧪 测试4: m-index.html 客服中心

### 步骤：

1. **保持在 m-index.html**（如果已关闭，重新打开并登录门店账号）

2. **打开消息中心**：
   - 点击底部导航的 "消息中心"

3. **查看客服中心**：
   - 页面应该显示 "客服中心" 标签
   - 如果有客户的咨询，会显示会话列表

4. **测试会话列表**：
   - 查看是否有会话
   - 点击任意会话打开聊天窗口

5. **测试聊天功能**（如果有会话）：
   - 在输入框输入消息
   - 点击发送按钮
   - 消息应该出现在聊天区域

6. **测试快捷回复**：
   - 点击快捷回复按钮
   - 选择预设回复内容

7. **预期结果**：
   - 客服中心正常显示
   - 聊天功能正常工作
   - 消息可以发送和接收

**预计时间**: 2分钟

---

## 📋 测试检查清单

### admin.html 订单管理
- [ ] 统计卡片显示正确
- [ ] 状态筛选正常工作
- [ ] 门店筛选正常工作
- [ ] 搜索功能正常
- [ ] 刷新按钮可以刷新数据
- [ ] 订单详情弹窗正常显示

### m-admin.html 账号切换
- [ ] 侧边栏显示 "切换账号" 按钮
- [ ] 侧边栏显示 "退出登录" 按钮
- [ ] 切换账号弹窗正常显示
- [ ] 选择不同账号后页面刷新
- [ ] 退出登录确认提示正常
- [ ] 退出后跳转到登录页

### m-index.html 订单流程
- [ ] 待处理订单显示正确
- [ ] "开始清洗" 按钮可以点击
- [ ] 状态正确更新到 "清洗中"
- [ ] "清洗完成" 按钮可以点击
- [ ] 状态正确更新到 "清洗完成"
- [ ] "通知取件" 按钮可以点击
- [ ] 状态正确更新到 "待取件"
- [ ] "确认取件" 按钮可以点击
- [ ] 状态正确更新到 "已完成"

### m-index.html 客服中心
- [ ] 客服中心标签正常显示
- [ ] 会话列表正常显示
- [ ] 可以打开聊天窗口
- [ ] 可以发送消息
- [ ] 快捷回复功能正常

---

## ❓ 常见问题

### Q1: admin.html 没有显示订单？
**A**: 
1. 先在 test-full-simulation.html 创建测试订单
2. 然后刷新 admin.html
3. 或点击 "刷新" 按钮

### Q2: m-admin.html 没有切换账号按钮？
**A**: 
1. 确保使用的是 m-admin.html
2. 打开侧边栏并向下滑动到底部
3. 应该在退出登录上方

### Q3: m-index.html 订单按钮不显示？
**A**: 
1. 确保登录的是门店账号
2. 确保订单状态是 "待处理"
3. 如果状态不对，使用测试工具重新创建订单

### Q4: 客服中心没有会话？
**A**: 
1. 这是正常的，C端用户还没有发起咨询
2. 可以在C端发起咨询后再测试

---

## 🎉 测试完成

如果所有测试都通过，恭喜你！订单功能和账号切换功能都已正常工作。

**测试结果汇总**：
- ✅ admin.html 订单管理
- ✅ m-admin.html 账号切换
- ✅ m-index.html 订单流程
- ✅ m-index.html 客服中心

---

## 📞 需要帮助？

如果测试过程中遇到问题：
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页的错误信息
3. 查看 Network 标签页的网络请求
4. 查看 Application/Storage 标签页的 localStorage 数据

---

**测试完成时间**: 2026-04-26  
**测试状态**: ✅ 完成
