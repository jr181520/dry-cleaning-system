# 智能灯条系统 - 完整测试流程

## 📋 测试目标

测试从 **客户端 (c-index)** → **移动端 (m-index)** → **客户端 (c-index)** 的完整流程，验证模拟 WLED 设备的状态同步和页面切换是否顺畅。

---

## 🚀 第一步：启动所有服务

### 方法 1：使用启动脚本（推荐）

双击运行：
```
启动全部服务.bat
```

### 方法 2：手动启动

**终端 1 - 启动 MQTT Broker**
```bash
cd d:\Trae CN\bin\dry_cleaning_system\backend
node start-mqtt-broker.js
```

**终端 2 - 启动后端服务**
```bash
cd d:\Trae CN\bin\dry_cleaning_system\backend
npm start
```

**验证服务状态**
- 打开浏览器访问：http://localhost:3000/api/health
- 应该返回：`{"status":"ok"}`

---

## 🧪 第二步：测试完整流程

### 场景 1：从客户端创建订单并触发灯条

**步骤 1.1** 打开客户端页面
```
浏览器打开：http://localhost:3000/c-index.html
```

**步骤 1.2** 登录（如果需要）
- 用户名：`test`
- 密码：`123456`

**步骤 1.3** 创建新订单
- 点击"新订单"按钮
- 填写订单信息
- 提交订单

**步骤 1.4** 观察灯条状态
- 灯条系统应该亮起，显示新订单
- 查看区域（如 A1）应该有指示灯

**预期结果**：
- ✅ 订单创建成功
- ✅ 灯条亮起（新订单指示）
- ✅ 后端收到 MQTT 消息

---

### 场景 2：从移动端查看和处理订单

**步骤 2.1** 打开移动端页面
```
新标签页打开：http://localhost:3000/m-index.html
```

**步骤 2.2** 打开侧边栏
- 点击左上角的菜单按钮（三条横线 ☰）
- 侧边栏应该从左边滑出

**步骤 2.3** 进入灯条系统
- 点击侧边栏中的"智能灯条系统"
- 应该显示灯条控制界面

**步骤 2.4** 查看模拟设备
- 应该有默认的 WLED 模拟设备（A1, A2, B1, B2 等）
- 选择一个设备（如 A1）
- 点击"开"按钮
- 应该显示模拟反馈

**步骤 2.5** 查看待取件列表
- 向下滚动，查看待取件订单
- 应该能看到刚才创建的订单

**预期结果**：
- ✅ 侧边栏正常打开
- ✅ 灯条系统界面正常显示
- ✅ 模拟设备可以控制
- ✅ 订单列表同步显示

---

### 场景 3：回到客户端确认

**步骤 3.1** 切换回客户端页面
- 点击客户端页面的标签

**步骤 3.2** 确认订单状态
- 查看订单列表
- 订单状态应该更新

**步骤 3.3** 测试页面切换流畅度
- 在客户端和移动端之间快速切换
- 检查页面是否响应正常

**预期结果**：
- ✅ 页面切换流畅
- ✅ 数据保持一致
- ✅ 无页面卡顿

---

## 🔍 第三步：检查 MQTT 通信

### 查看 MQTT Broker 状态

**方法 1：使用 MQTT.fx 工具**
1. 下载 MQTT.fx：https://mqttfx.org/
2. 连接配置：
   - Broker Address: localhost
   - Broker Port: 1883
   - Protocol: MQTT

**方法 2：查看后端日志**

在后端服务终端查看日志，应该能看到：
```
[MQTT] 客户端连接: clientId
[MQTT] 订阅成功: dryclean/+/+/light/status
[MQTT] 收到消息: dryclean/prod/A1/light/control
```

### 监控 MQTT 主题

使用 mosquitto 客户端订阅主题：
```bash
# 订阅所有灯条状态
mosquitto_sub -t "dryclean/#" -v

# 只订阅控制命令
mosquitto_sub -t "dryclean/+/+/light/control" -v

# 只订阅状态更新
mosquitto_sub -t "dryclean/+/+/light/status" -v
```

---

## 📊 第四步：测试各种功能

### 4.1 灯条控制功能

| 功能 | 测试方法 | 预期结果 |
|------|---------|---------|
| 开灯 | 选择设备 → 点击"开" | 模拟反馈显示"灯已开启" |
| 关灯 | 选择设备 → 点击"关" | 模拟反馈显示"灯已关闭" |
| 颜色切换 | 选择颜色（红/绿/蓝/黄/紫） | 模拟反馈显示"颜色已设置为 xxx" |
| 亮度调节 | 拖动亮度滑块 → 点击"应用" | 模拟反馈显示"亮度已设置为 xxx" |
| 预设效果 | 点击效果按钮（如"呼吸"） | 模拟反馈显示"效果已设置为 xxx" |

### 4.2 设备管理功能

| 功能 | 测试方法 | 预期结果 |
|------|---------|---------|
| 添加设备 | 点击"添加设备" → 填写信息 → 点击"添加" | 设备列表中出现新设备 |
| 编辑设备 | 点击设备 → 点击"编辑" → 修改信息 → 点击"保存" | 设备信息更新 |
| 删除设备 | 点击设备 → 点击"删除" → 确认删除 | 设备从列表中移除 |

### 4.3 订单流程

| 功能 | 测试方法 | 预期结果 |
|------|---------|---------|
| 创建订单 | 点击"新订单" → 填写信息 → 提交 | 订单创建成功，灯条亮起 |
| 查看订单 | 在订单列表中查看 | 订单状态正确显示 |
| 更新状态 | 点击订单 → 选择状态 → 保存 | 状态更新，灯条指示变化 |

---

## 🐛 第五步：排查问题

### 问题 1：侧边栏无法打开

**检查项**：
- [ ] 浏览器控制台是否有错误（按 F12）
- [ ] Tailwind CSS 是否加载成功
- [ ] JavaScript 是否执行

**解决方案**：
- 刷新页面
- 清除浏览器缓存
- 使用离线版本：`m-index-offline.html`

### 问题 2：灯条不响应

**检查项**：
- [ ] MQTT Broker 是否运行
- [ ] 后端服务是否运行
- [ ] 模拟模式是否开启

**解决方案**：
- 确保模拟模式已勾选
- 检查后端控制台日志
- 确认 MQTT Broker 在端口 1883 监听

### 问题 3：订单不同步

**检查项**：
- [ ] 后端 API 是否正常
- [ ] 浏览器网络请求是否有错误

**解决方案**：
- 检查浏览器 Network 标签
- 查看后端日志
- 确认数据库连接正常

---

## ✅ 第六步：测试完成检查清单

在测试过程中，每完成一项就打勾：

### 服务状态
- [ ] MQTT Broker 已启动（端口 1883）
- [ ] 后端服务已启动（端口 3000）
- [ ] 可以访问 http://localhost:3000/api/health

### 客户端页面 (c-index.html)
- [ ] 页面正常加载
- [ ] 可以登录
- [ ] 可以创建订单
- [ ] 订单列表正常显示

### 移动端页面 (m-index.html)
- [ ] 页面正常加载
- [ ] 侧边栏可以打开
- [ ] 侧边栏可以关闭
- [ ] 可以进入灯条系统
- [ ] 模拟设备可以控制

### 灯条系统
- [ ] 可以开关灯条
- [ ] 可以切换颜色
- [ ] 可以调节亮度
- [ ] 可以切换效果
- [ ] 模拟反馈正常显示

### 页面切换
- [ ] 从 c-index 切换到 m-index 流畅
- [ ] 从 m-index 切换到 c-index 流畅
- [ ] 数据保持同步

### MQTT 通信
- [ ] 后端成功连接 MQTT Broker
- [ ] 可以收到灯条控制消息
- [ ] 可以发送灯条状态消息

---

## 📝 测试日志模板

```
测试日期：_______________
测试人员：_______________

服务状态：
- MQTT Broker: ✓ / ✗
- 后端服务: ✓ / ✗

功能测试：
1. 侧边栏打开/关闭: ✓ / ✗
2. 灯条开关: ✓ / ✗
3. 颜色切换: ✓ / ✗
4. 亮度调节: ✓ / ✗
5. 订单创建: ✓ / ✗
6. 页面切换: ✓ / ✗

遇到的问题：
1. _________________________________
2. _________________________________
3. _________________________________

解决方案：
1. _________________________________
2. _________________________________
3. _________________________________

总体评价：
□ 优秀 - 所有功能正常
□ 良好 - 有小问题但不影响使用
□ 一般 - 有较大问题需要修复
□ 较差 - 功能不可用

备注：
_________________________________
```

---

## 🎯 测试成功标准

**最低要求（必须通过）**：
- ✅ 侧边栏可以打开和关闭
- ✅ 模拟灯条可以控制
- ✅ 可以在 c-index 和 m-index 之间切换
- ✅ 服务不崩溃

**良好标准**：
- ✅ 所有灯条控制功能正常
- ✅ 订单流程完整
- ✅ MQTT 通信正常
- ✅ 页面响应流畅

**优秀标准**：
- ✅ 所有功能完美运行
- ✅ 无控制台错误
- ✅ 数据完全同步
- ✅ 用户体验流畅

---

## 📞 遇到问题？

如果在测试过程中遇到任何问题：
1. 记录错误信息（截图或复制错误文本）
2. 查看浏览器控制台（F12）
3. 查看后端服务终端日志
4. 尝试刷新页面或重启服务

---

**祝你测试愉快！🎉**
