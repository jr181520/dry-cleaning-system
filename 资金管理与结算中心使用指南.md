# 资金管理与结算中心使用指南

## 功能概述

在连锁管理端新增了资金管理和结算中心功能，允许连锁企业管理下辖门店的资金流水、账户余额、结算对账等功能。

## 新增API接口

### 资金管理API

1. **资金概览**
   ```
   GET /api/v1/admin/chains/:chainId/finance/overview
   ```
   返回连锁企业的资金概览数据，包括总金额、待结算金额、已结算金额、今日变动等。

2. **资金流水记录**
   ```
   GET /api/v1/admin/chains/:chainId/finance/records
   参数：page, pageSize, type, startDate, endDate
   ```
   返回资金流水记录，支持按类型和时间筛选。

3. **门店资金统计**
   ```
   GET /api/v1/admin/chains/:chainId/finance/stores
   ```
   返回各门店的资金统计，包括营收、订单数、结算比例等。

4. **资金趋势**
   ```
   GET /api/v1/admin/chains/:chainId/finance/trend
   ```
   返回资金趋势数据，包括日趋势和月趋势。

### 结算中心API

1. **结算概览**
   ```
   GET /api/v1/admin/chains/:chainId/settlement/overview
   ```
   返回结算概览数据，包括待处理结算单数量、已完成结算单数量、总结算金额等。

2. **结算单列表**
   ```
   GET /api/v1/admin/chains/:chainId/settlements
   参数：page, pageSize, status
   ```
   返回结算单列表，支持按状态筛选。

3. **创建结算单**
   ```
   POST /api/v1/admin/chains/:chainId/settlements/create
   请求体：{ period, storeId, ratio, remark }
   ```
   创建新的结算单，可以按门店或全部门店生成结算。

## 前端页面

### 资金管理页面
- **路径**: `chain-admin.html#page-finance`
- **功能**:
  - 资金概览卡片显示关键指标
  - 资金流水表格展示所有资金变动
  - 门店资金排名表格
  - 资金趋势图表

### 结算中心页面
- **路径**: `chain-admin.html#page-settlement`
- **功能**:
  - 结算概览卡片
  - 最近结算单列表
  - 结算单明细表格
  - 创建结算单表单

## 数据库模型

### 新增结算模型（Settlement）
```javascript
{
  settlementNo: String,        // 结算单号
  chainId: String,             // 连锁ID
  storeId: String,             // 门店ID
  period: String,              // 结算周期
  amount: Number,              // 结算金额
  ratio: Number,               // 结算比例
  status: String,              // 状态：pending/paid/cancelled
  remark: String,              // 备注
  createdBy: String,           // 创建人ID
  createdAt: Date,             // 创建时间
  settlementDate: Date,        // 结算时间
  paidAt: Date,                // 支付时间
  orders: [{
    orderId: String,           // 订单ID
    orderNo: String,           // 订单号
    amount: Number,            // 订单金额
    orderDate: Date            // 订单日期
  }]
}
```

## 使用步骤

### 1. 访问连锁管理端
打开 `chain-admin.html` 并使用有 `chain_admin` 权限的账户登录。

### 2. 查看资金管理
- 点击左侧导航栏的"财务管理" → "资金管理"
- 查看资金概览、流水记录、门店资金统计和趋势图表

### 3. 使用结算中心
- 点击左侧导航栏的"财务管理" → "结算中心"
- 查看结算概览和最近结算单
- 使用右侧表单创建新的结算单：
  - 选择结算周期（日结/周结/月结）
  - 选择门店（或全部门店）
  - 设置结算比例（1-100%）
  - 点击"生成结算单"

### 4. 监控资金变动
- 资金流水记录会自动更新
- 门店资金统计会实时反映最新的营收数据
- 趋势图表显示资金变化趋势

## 注意事项

1. **权限要求**: 需要 `admin` 或 `chain_admin` 角色才能访问这些功能
2. **数据安全**: 资金数据涉及财务信息，确保只有授权人员可以访问
3. **结算逻辑**: 结算单创建后会自动更新相关订单的结算状态
4. **测试建议**: 在正式使用前，建议在测试环境下验证功能

## 故障排查

### 常见问题

1. **API返回404错误**
   - 检查路由是否正确配置
   - 确认后端服务已启动
   - 验证认证token是否有效

2. **数据不显示**
   - 检查连锁ID是否正确
   - 确认数据库中有相关订单数据
   - 查看浏览器控制台是否有错误信息

3. **结算单创建失败**
   - 检查门店是否有待结算的订单
   - 验证结算比例是否在有效范围内
   - 查看后端日志获取详细错误信息

### 调试方法

1. 使用浏览器开发者工具查看网络请求
2. 检查后端日志中的错误信息
3. 使用测试脚本验证API功能

## 相关文件

1. **后端路由**: `backend/modules/admin/routes/adminRoutes.js`
2. **业务逻辑**: `backend/modules/admin/services/adminService.js`
3. **前端页面**: `chain-admin.html`
4. **测试脚本**: `测试资金管理API.js`

## 后续优化建议

1. 添加资金提现功能
2. 实现自动结算定时任务
3. 增加财务报表导出功能
4. 添加资金预警机制
5. 集成第三方支付渠道