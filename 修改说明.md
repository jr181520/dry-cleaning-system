# 待手动添加的灯条触发功能

## C端订单详情页（c-order-detail.html）

### 位置：第1864行之后，在 `localStorage.setItem('currentOrder', JSON.stringify(currentOrder));` 之后

### 需要添加的代码：

```javascript
// 触发灯条请求
if (typeof triggerLightRequest === 'function') {
  triggerLightRequest();
}
```

### 需要添加的新函数（在 `// 提交取件方式并支付配送费` 之前）：

```javascript
// 触发灯条请求
async function triggerLightRequest() {
  if (!currentOrder) return;
  const storeId = currentOrder.storeId || currentOrder.store?.id || 'ST001';
  const items = currentOrder.items || currentOrder.services || [];
  
  try {
    for (let i = 0; i < items.length; i++) {
      const item = items[i];
      await fetch('http://localhost:3000/api/store/order-light/request', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          orderId: orderId,
          storeId: storeId,
          itemIndex: i,
          itemName: item.name || item.serviceName || '物品' + (i + 1),
          requestType: 'customer_scan',
          customerPhone: localStorage.getItem('userPhone') || 'C端用户',
          timestamp: Date.now()
        })
      });
    }
    
    const lightRequest = {
      orderId: orderId,
      storeId: storeId,
      itemIndex: 0,
      itemName: items[0]?.name || items[0]?.serviceName || '物品',
      requestType: 'customer_scan',
      customerPhone: localStorage.getItem('userPhone') || 'C端用户',
      timestamp: Date.now()
    };
    localStorage.setItem('light_request_' + orderId, JSON.stringify(lightRequest));
    window.dispatchEvent(new CustomEvent('lightRequestFromC', { detail: lightRequest }));
    alert('✅ 已通知店员，灯条正在点亮...\n\n请等待店员确认取件。');
  } catch (error) {
    console.error('触发灯条请求失败:', error);
  }
}
```

## 功能说明

当用户在C端订单详情页点击"扫码取件"按钮时：
1. 会调用后端API `/api/store/order-light/request` 发送灯条请求
2. 同时保存到localStorage
3. 触发自定义事件 `lightRequestFromC`
4. M端会监听这些事件并显示灯条弹窗

## 已完成的修改

✅ M端灯条窗口关闭按钮已修复
✅ C端首页扫码取件入口已有灯条触发功能（跳转到c-pickup-store.html）
